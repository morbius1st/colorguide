# colorguide
# view info fields

module.exports =
class cgViewInfo

  constructor: (title, prefix, folder, fileName, version) ->
    @title = title
    @prefix = prefix
    @folder = folder
    @fileName = fileName
    @version = version
